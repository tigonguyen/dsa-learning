#include <iostream>
#include <map>

using namespace std;

int testCases;

int main() {
    cin >> testCases;
    for (int test = 0; test < testCases; test++ ) {
        int n, k;
        cin >> n >> k;
        for (int i = 0; i < n; i++) {
            
        }
    }
    return 0;
}